import { FC } from 'react';

import { SignOutButton } from '@/containers/signOutButton/SignOutButton';

const Home: FC = async () => {
    return (
        <div>
            <SignOutButton />
        </div>
    );
};
export default Home;
