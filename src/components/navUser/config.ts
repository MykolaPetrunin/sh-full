import { BadgeCheck } from 'lucide-react';

import { MenuItem } from '@/components/navUser/types';
import { navUserTexts } from '@/components/navUser/texts';

export const menuItems: MenuItem[] = [{ title: navUserTexts.items.account, Icon: BadgeCheck }];
